# Fresh-home public bootstrap

The documented sync dry-run, apply and check each exited 0 in an isolated home with no private instruction files. Both managed roots expose all actual enumerated canonical targets and readable SKILL.md files (installation.json). No user-owned live roots or instructions were touched.

A single native Codex recommendation-only workflow attempt used the literal saved prompt through stdin, a clean child environment, no copied credentials, and the isolated home. It exited 1 after its built-in transport retries reported HTTP 401. Native skill selection and literal echo remain unavailable. Installation/link discovery succeeded; authenticated execution did not. The CLI additionally warned that helper aliases cannot be created under /tmp. Complete stdout/stderr and original command/input digests are preserved.

Confidence: 98% for these installation and attempt observations; no native discovery or authenticated workflow success is inferred. Owner-supplied provider access remains separate from installation correctness.

## Native catalog completion

A later local app-server probe used the installed Codex protocol schema and an initialize → skills/list request on stdio in the same isolated home. Native discovery returned all 31 expected canonical skills enabled, no missing names or schema errors, and exited 0. The full native catalog contains six additional system skills. Input messages, native responses, schema inputs, source digests and the exact process command are preserved. No model or authentication is needed for this catalog operation. This supersedes only the earlier unavailable catalog observation, not its HTTP 401 workflow outcome or unavailable literal echo.

Claude and Cursor were each also attempted once against an explicit synthetic existing-Claude-layout profile in the isolated home. Claude reported Not logged in with measured zero usage/cost; Cursor reported Authentication required. Both exited 1. These are verified unavailable native compatibility attempts; they are not evidence of successful cross-provider recording. No real user store or credential was read/copied.
