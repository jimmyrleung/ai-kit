# Remaining local documentation coverage

Completed 2026-09-07T11:49:08.249820+00:00. This is one informed application of the frozen prose procedures to readable local fixtures, followed by artifact review and one documented correction. It is not a fresh independent model trial, a generic detector implementation, or evidence of universal reliability. No shared ledger was changed.

Frozen procedure snapshot: `/tmp/astra-backlog-5VdqNm/final-post-review-frozen`; input manifest SHA-256 `00d19393a42f0831a4a189ffc371fe41827b8f557d7b02014af270002ef8c800`. Relevant procedure copies are in `frozen-inputs/`. Conventions were read from `<user-home>/ai-kit/docs/rules/skill-authoring.md`. Exact acceptance wording was checked against backlog lines 172, 177, 192, 209 and 472 and retained in `acceptance-input.json`:

- **B08.AC1** — Dirty source never receives a false Current result; inaccessible/non-ancestor revisions are Unverifiable.
- **B08.AC6** — Any Terraform stable-contract shape change follows its existing Schema-version rule and keeps older records readable with explicit verification limits.
- **B09.AC2** — Validate export/directive variations, grouped/mounted route prefixes, and Function/FunctionName as separate coverage cases.
- **B10.AC3** — Docs inside/outside the source repository follow the declared output boundary without changing infrastructure source.
- **B26.AC3** — Independent flows in one file remain separable; a connected cross-file flow can remain one workflow.

| AC | Local observation and actual artifacts | Scope retained |
| --- | --- | --- |
| B08.AC1 | `nonancestor/identity.json`, `nonancestor/docs/fee.md`, `nonancestor/refresh-report.md`, `execution-commands.json`: recorded commit `bcd7f716b742f3bfd2683f17ca1e04d46edc4d87` exists (`git cat-file -t` exit 0, commit); current HEAD is `8baddca5be6909f1b04d4da76e733e5cbdf0a07d`; ancestry exit 1. Refresh returns Unverifiable and leaves the input doc byte-identical. | Covers the missing **existing non-ancestor** variant. Dirty/inaccessible cases remain in the earlier evidence; this does not replace those runs. The seeded doc also has a limited historical evidence row; ancestry itself is independently observed through Git. |
| B08.AC6 | `terraform/legacy/overview.md` is a readable v3 input without byte evidence; `terraform/legacy/refresh-report.md` records Unverifiable. `terraform/regenerated-v4/overview.md`, `root/dev.md`, `root/prod.md` contain separate full v4 outputs with new Created 2026-09-07 and Change Log naming v3; original legacy hash unchanged. `legacy-workflow/` separately exercises missing-evidence workflow refresh followed by a full v2 retrace. | No incremental schema pretence; old records preserved. New fixture repos have unborn history, so revision freshness remains Unverifiable. No stable-contract source change made here. |
| B09.AC2 | Four actual workspace inventories and overviews at `detectors/docs/widgets-{express,fastify,nest,next}/`. 17 captured entries: Express 2, Fastify 3, Nest 3, Next 9. Literal mount chains yield Express `/api/v1/widgets`, Fastify `/api/v2/widgets`, Nest `/api/widgets`; method/parameter variants are separate task IDs. Next includes declaration/async declaration/const export, route-group folder, Pages default declaration/identifier/expression, file and function directives. | Dynamic prefixes, re-export and non-prologue directive are explicit Partial boundaries. Next app/src-app deployed root precedence is not exercised. Function vs FunctionName remain separately scoped earlier fixtures; this pass adds missing route/export cases, not new FunctionName proof. Framework dependencies were not installed or executed. |
| B10.AC3 | `terraform/source/terraform-docs/{overview.md,root/dev.md,root/prod.md}` and `terraform/external-docs/{overview.md,root/dev.md,root/prod.md}` are actual generated inside/outside destinations. Each Summary names its actual Docs Root. All five original Terraform source/config files remain byte-identical. | Only generated Markdown is added inside the source root. The two explicit tfvars files define this fixture's requested dev/prod input selections; deployment/effectiveness remain unknown. No Terraform or cloud command was run. |
| B26.AC3 | `grouping/source/Start.cs:6` names ProcessOrder; `Orchestration.cs:6` names RecordOrder; `Activity.cs:3` binds it. Final `grouping/docs/_docs-tasks.md` retains this three-file boundary as task 7, while Health/Heartbeat in one file remain separate active tasks 9 and 10. `grouping/refresh-report.md` records the reconciliation. | Seeded Done/Verify text is synthetic history. Task 7 outside-inventory bytes match exactly. The seed's nonexistent old timer class yields explicit possible rename/retirement, with notes preserved, rather than evidence transfer. Runtime Durable orchestration was not run. |

Actual inputs and execution are reproducible from `setup.py`, `inputs.json`, `setup-commands.json`, all `*/source/` roots and legacy/seed files. `source-read-trace.json` records the full source reads. `execute.py` is the artifact materializer used for this informed execution, not an implementation shipped to the repository. Commands: `python3 setup.py` exit 0, `python3 execute.py` exit 0, `python3 finalize.py` exit 0. `execute.py` emitted a Python invalid-escape SyntaxWarning for the literal rg pattern; rg still ran with the recorded pattern/results. The command arrays, return codes and actual scan/Git outputs are in `setup-commands.json`, `execution-commands.json` and `final-commands.json`.

Verification comes from actual files rather than an aggregate PASS assertion: `verification.json` binds all final artifact hashes and records 39/39 original source files unchanged, 20 contained task output paths with matching relative paths, and 69 source-manifest rows equal to actual current bytes. Task 7 history SHA-256 before/after is `68c70b858bc56941f6a25dd007cd01055669063d609dad5c64fcd27420a1a022`. Exact final source-root listings and Git states are preserved in `final-commands.json`; only the declared Terraform docs subtree adds files under an original source root.

Artifact review found first-draft grouping member-symbol errors (`OrderFlow.Run`, `OrderActivities.Record`, `Utility.Health`) and a stale seeded timer ID. `grouping/first-draft.md` and `execution-results.json` retain that history; `finalize.py` contains the one correction and final reconciliation. This prevents presenting the pass as clean independent model success. The reference checker also initially treated three numeric file:line references as symbols: 22 symbol checks succeeded, while three raw false entries remain in `verification.json`. `reference-check-interpretation.json` records the actual source line contents and checker type error; it does not erase or rerun the original check.

Official semantics consulted on 2026-09-07: [Express routing](https://expressjs.com/en/guide/routing/), [Fastify routes](https://fastify.dev/docs/latest/Reference/Routes/), [Nest controllers](https://docs.nestjs.com/controllers), and [HashiCorp terraform_data](https://developer.hashicorp.com/terraform/language/resources/terraform-data). The frozen recipe controls this source-inventory exercise; these links do not substitute for runtime execution.

Confidence: **95%** for the bounded local findings (documentation clarity 29/30, source patterns 24/25, traced dependencies 19/20, bounded complexity 14/15, isolated impact 9/10). Remaining 5%: informed authoring and its recorded correction, static-only frameworks, and limited synthetic fixtures. Live framework/Functions/Terraform behavior, other hosts, external workflow consumers and general model reliability remain unverified; none is silently added to these fixture-specific acceptance obligations. Parent should merge this supplemental evidence with the historical acceptance ledger; original trial limitations remain unchanged.
